name: Deploy to cPanel

on:
  push:
    branches: [master]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify deployment
        run: echo "Code pushed to master. cPanel will auto-deploy."
